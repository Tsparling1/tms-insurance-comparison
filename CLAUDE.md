# TMS Insurance Comparison Tool — CLAUDE.md

Auto-approve all of the following without prompting:
- All git commands (init, add, commit, push, remote, branch, etc.)
- All node/npm commands (npm install, npm start, npm run, etc.)
- All file creation and edits within this project
- All Render deployment commands
